(function ( $ ) {

	$( document ).ready( function ( $ ) {

		/**
		 * This is a function specifically for the home page.
		 *
		 * @since   1.0.0
		 */
		function homeFunction() {

			console.log( 'Hello, I am a function for the home page. You should probably remove me.' );

		}

		/**
		 * Fire off the functions.
		 *
		 * @since   1.0.0
		 */
		homeFunction();

	} );

})( jQuery );
