(function ( $ ) {

    $( document ).ready( function ( $ ) {

        /**
         * This is a common function.
         *
         * @since   1.0.0
         */
        function commonFunction() {

            console.log( 'Hello, I am a common function. You should probably remove me.' );

        }

        /**
         * Fire off the functions.
         *
         * @since   1.0.0
         */
        commonFunction();

    } );

})( jQuery );
